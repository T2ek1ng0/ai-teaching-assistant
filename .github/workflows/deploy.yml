name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # 确保这是你的主分支名称

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18' # 或者你项目中使用的Node.js版本

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
<<<<<<< HEAD
          publish_dir: ./dist
=======
          publish_dir: ./dist
>>>>>>> e95ab12641dcd8c6a054871952fb315ac3834fc9
